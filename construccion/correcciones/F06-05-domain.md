# F06-05 · revisión independiente de dominio y SQL

Controles287 externos al candidato, base08a4b09d. SQL0026 extiende intervenciones/planes canónicos y conserva las guardas legacy; no aplica SQL remoto. Puros5 Node22/26 verifican definición obligatoria, fechas UTC válidas, estados y roles, incluida reapertura owner. Mutante de operador sin asignación muere por aserción semántica y se restaura.

El examen SQL comprueba RLS forzada, grants privados,11FK con positivos y referencias ajenas, historia inmutable, permisos vigentes y capabilities acotadas. Captura planes antes de aprobar; fuente/scope/contexto y baseline de medición permanecen distintos. Estado/CAS, atribución del actor y datos de aprobación no pueden alterarse mediante SQL directo.

La medición positiva usa publicaciones sintéticas con ventanas UTC de un segundo y reloj real:9007199254740993→9007199254741000 da delta7 exacto. Los resultados arbitrarios del caller son reemplazados por cálculo canónico. Reembolsos2000 menos reverso500 dan1500; un reverso desconocido no mantiene el importe íntegro conocido. Datos parciales o inmaduros conservan null/razones y no cierran. No hay ahorro causal; intervención no ejecuta CRM ni pagos.

La reasignación owner conserva plan/expectativas e historial después de revocar al operador histórico; nunca rehabilita una fuente retirada. La reapertura conserva resultados anteriores pero incrementa un ciclo propiedad de DB. Un resultado antiguo no puede adjuntarse para cerrar de nuevo; insertar una medición nueva autorizada sí permite cierre. Un mutante que convierte el delta financiero en cero muere por aserción semántica0→1→0.

## Hallazgos y recuperación

Se conservaron rojos de producto: proyección directa devolvía dinero con fuente retirada; captura de eventos rompía borradores025 sin fingerprint opcional; referencia SQL a customer.active inexistente; precedencia JSON scope incorrecta; reapertura permitía volver a adjuntar resultado antiguo. Correcciones mínimas probadas antes de la matriz final. El último caso usa measurement_cycle inmutable de DB y captura del ciclo en cada resultado.

Errores de fixtures se separan de producto: UPDATE oculto por RLS devuelve cero filas legítimamente; SELECT result necesitó alias para evitar variable PL/pgSQL homónima; semilla de ventana futura conserva watermark real, sin afirmar observación futura; el resultado nuevo usa actor actual, no copia actor histórico. Logs originales preservados. Ninguna corrección desactiva guardas de producto. FK usa el aislamiento transaccional habitual del examen para aislar integridad referencial, sin atribuir ese bypass a autorización.

Los recibos privados contienen hashes, SQLchain26, comandos, logs y cleanup por IDs. La matriz global se ejecuta una vez tras focales del hash final; su aprobación y la aceptación limpia corresponden al principal. Los controles no acreditan despliegue ni producción.

## Revocación del contribuyente de dimensiones

Un probe privado posterior al freeze reprodujo lectura de un resultado con mappingIds capturados de otro owner ya revocado, mientras el propietario financiero y responsable operativo permanecían vigentes. Se conserva el positivo anterior a la revocación y se exige denegación posterior, sin confundir responsabilidad histórica con autorización de evidencia. El control se extiende en copia disjunta; no se modifica el examen de la matriz ya en curso.
