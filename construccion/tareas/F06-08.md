# F06-08 — Centro in-app y preferencias para usuarios VEXA con membership/RLS; no contactos CRM.

**Modalidad:** worker. **Dependencias:** F01-03, F01-04, F02-06.
**Gate:** PRESENT (no significa PASS).

## Entrada (leer, no inventar contexto)
- `AGENTS.md`
- `docs/CONTEXTO-CANONICO.md`
- `docs/blueprint/F06.md`
- `construccion/03-CONTRATOS.md`

## Salida prevista — no afirma que ya existe
- `packages/notifications/OPERATIONS.md`
- `packages/notifications/catalog.mjs`
- `packages/notifications/contracts.mjs`
- `packages/notifications/index.d.mts`
- `packages/notifications/index.mjs`
- `packages/platform/src/db.ts`
- `supabase/migrations/0028_notification_center.sql`
- `apps/web/src/lib/notifications/server.ts`
- `apps/web/src/app/api/notifications/route.ts`
- `apps/web/src/app/api/notifications/preferences/route.ts`
- `apps/web/src/app/api/notifications/[id]/read/route.ts`
- `apps/web/src/components/notifications/panel.tsx`
- `apps/web/src/app/(workspace)/notifications/page.tsx`
- `apps/web/src/app/(workspace)/settings/notifications/page.tsx`
- `apps/web/src/components/workspace/navigation.tsx`

## Pasos específicos
1. Leer PROPUESTAS-PARA-INTEGRAR y reutilizar catálogo/eligibilidad/templates revisados.
2. Centro in-app y preferencias por tenant/usuario/evento/canal, FK a memberships y RLS; nunca destinatarios desde clientes CRM.
3. API revalida permisos del recurso y membership vigente; ausencia de configuración falla cerrada.
4. Invitaciones requieren acto explícito de owner y destinatario reservado; marcar eventos no conectados como tales.

## Oráculos obligatorios para el gate
- A no lee ni modifica inbox/preferencias de B mediante REST directo.
- Viewer sólo cambia sus propias preferencias y lectura.
- Un customer importado sin membership jamás se convierte en destinatario.

## Preparar la prueba ANTES del worker
1. Controlador escribe el gate siguiente a partir de los oráculos de arriba. Si requiere DB/browser, seguir 04-PRUEBAS-Y-LOOP.md; no sustituirlo por un JSON que diga passed.
2. Ejecutar contra implementación ausente y contra el defecto específico (nulo→cero, duplicado, fuga, según tarea). Registrar fallo por la aserción correcta; un error de setup NO mata un mutante.
3. Revisar gate fuera del candidato y hacer commit. Refrescar registro de gates y guía al agregar pruebas, antes de prepare, nunca durante una verificación.

## Comandos del ciclo
```bash
cd ~/vexa
python3 scripts/guide.py audit
python3 scripts/guide.py packet --task F06-08
python3 orchestration/runner.py prepare --task F06-08 --max-minutes 1
# Editar sólo el worktree impreso, sin commit; guardar su ruta en CANDIDATE.
VEXA_CANDIDATE="$CANDIDATE" node --test tests/acceptance/F06-08.test.mjs
python3 orchestration/runner.py verify --task F06-08 --max-minutes 5
# Revisión independiente de diff, controles y log; NO aceptar por autoafirmación.
# Si se rechaza: STOP + reject --task F06-08 --approval-note "referencia real"; ver capítulo 01.
python3 orchestration/runner.py accept --task F06-08 --max-minutes 5
```
Estos comandos fallan cerrados si faltan gate/dependencias/permiso; no son evidencia de ejecución. No pegar la nota de ejemplo como aprobación. La modalidad external requiere actos fuera del loop; prepare sólo abre un candidato local.
Alternativa offline una vez listo gate/entorno: `python3 orchestration/runner.py run --max-rounds 1 --max-minutes 20`. Selecciona la próxima tarea elegible del grafo; no garantiza que sea esta. Verificar estado antes.

## Recuperación específica
Conservar filas y error; no reemplazar autorizador por snapshot enviado por browser.

## Cierre obligatorio
`task_id, baseline_sha, candidate_sha, fixture_hash, command, exit_code, expected, observed, artifacts, reviewer, pending`. Las pruebas/grafo no las edita el candidato. Dos fallos agotan intento; guardar reproducción y pasar a recuperación supervisada, no editar status=accepted. Build/instalación de dependencias se hacen en copia temporal para no mutar el candidato durante el gate.
