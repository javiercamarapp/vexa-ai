# F08-02 — Ejecutar smoke remoto del flujo completo, dos tenants, job durable y export.

**Modalidad:** external. **Dependencias:** F08-01.
**Gate:** AUTHORED — control independiente integrado; no implica aprobación remota.

## Entrada (leer, no inventar contexto)
- `AGENTS.md`
- `docs/CONTEXTO-CANONICO.md`
- `docs/blueprint/F08.md`
- `construccion/03-CONTRATOS.md`

## Salida prevista — no afirma que ya existe
- Recibo y evidencias privados externos al checkout; resumen sanitizado posterior, separado del SHA probado.

## Pasos específicos
1. Desde URL autorizada, leer SHA servido, iniciar sesión A/B de ensayo e importar SYN.
2. Observar cola, worker y DB reales hasta estado terminal; navegar ocho rutas y descargar export protegido.
3. Revocar A e intentar lectura y export; inducir fallo limitado del consumidor y observar alarma.
4. Guardar trace ID, expected/observed y screenshots sin PII.

## Oráculos obligatorios para el gate
- Exposición 30000 y refund 1500 coinciden en UI, API y export.
- A no lee B, antes ni después de revocar acceso.
- Apagar consumidor no convierte 202 en éxito ni pierde jobs silenciosamente.

## Preparar la prueba ANTES del worker
1. Controlador escribe el gate siguiente a partir de los oráculos de arriba. Si requiere DB/browser, seguir 04-PRUEBAS-Y-LOOP.md; no sustituirlo por un JSON que diga passed.
2. Ejecutar contra implementación ausente y contra el defecto específico (nulo→cero, duplicado, fuga, según tarea). Registrar fallo por la aserción correcta; un error de setup NO mata un mutante.
3. Revisar gate fuera del candidato y hacer commit. Refrescar registro de gates y guía al agregar pruebas, antes de prepare, nunca durante una verificación.

## Comandos del ciclo
```bash
cd ~/vexa
python3 scripts/guide.py audit
python3 scripts/guide.py packet --task F08-02
python3 orchestration/runner.py prepare --task F08-02 --max-minutes 1
# Guardar la ruta en CANDIDATE. Esta aceptación examina el SHA publicado SIN editar el candidato.
# Ejecutar primero el launcher confiable conforme support/F08-smoke/README.md, fuera del runner.
# Pasar sólo VEXA_SMOKE_GATE_INPUT; la referencia procede de la decisión legítima guardada.
VEXA_CANDIDATE="$CANDIDATE" node --test tests/acceptance/F08-02.test.mjs
python3 orchestration/runner.py verify --task F08-02 --max-minutes 5 --approval-note "REFERENCIA A LA DECISIÓN REAL DEL OPERADOR"
# Revisión independiente de diff, controles y log; NO aceptar por autoafirmación.
# Si se rechaza: STOP + reject --task F08-02 --approval-note "referencia real"; ver capítulo 01.
python3 orchestration/runner.py accept --task F08-02 --max-minutes 5
```
Estos comandos fallan cerrados si faltan gate/dependencias/permiso; no son evidencia de ejecución. No pegar la nota de ejemplo como aprobación. La modalidad external requiere actos fuera del loop; prepare sólo abre un candidato local.

## Recuperación específica
Rollback al SHA compatible anterior y repetir smoke; no promover sólo porque pasó CI local.

## Cierre obligatorio
`task_id, baseline_sha, candidate_sha, fixture_hash, command, exit_code, expected, observed, artifacts, reviewer, pending`. Las pruebas/grafo no las edita el candidato. Dos fallos agotan intento; guardar reproducción y pasar a recuperación supervisada, no editar status=accepted. Build/instalación de dependencias se hacen en copia temporal para no mutar el candidato durante el gate.

## Control integrado —25-sep

Fuentes revisadas por363 y delta de aislamiento del supervisor por364. Véase [reglas del gate](../correcciones/F08-02-gate.md). Los artefactos quedan fuera del candidato; añadir un reporte al checkout durante verify cambiaría el SHA y exigiría un ensayo nuevo. El gate corto nunca lanza remoto ni sustituye la prueba por un JSON favorable.
