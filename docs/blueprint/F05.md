# F05 — Dinero, snapshots y priorización

Días objetivo: 12–18. Dependencias: F03, F04. Estado: especificado, no ejecutado.

## Entrada obligatoria
Lee ../CONTEXTO-CANONICO.md, 00-BLUEPRINT-MAESTRO.md, 01-CONTRATOS-Y-DATOS.md y calidad/*.md pertinente. Consulta ../investigacion/integraciones/ para contratos y spikes. Si esta fase se abre en sesión nueva, estos archivos contienen el contexto necesario.

## Resultado
Extender kernel financiero puro a ledger/consultas SQL y snapshots; ranking reproducible y con costos separados.

## Pasos
1. Leer contrato completo calidad/01 y kernel existente, no reescribir reglas por gusto.
2. Separar revenue exposure, refunds netos, replacement cost, costo modelado soporte y escenarios de future loss.
3. Unión global de órdenes/clientes/eventos entre problemas; filas no aditivas señaladas.
4. Minor units/Decimal, moneda/exponente/horizonte/FX versionados; unknown!=zero y coverage.
5. Publish snapshot atómico con scope_hash, versiones y watermark; export/UI mismo bundle.
6. Prioridad con carril crítico aparte, fórmula versionada y razones; normalización no oculta urgencias al cambiar set.

## Criterios observables
- Todos FIN-01..12 y contraejemplos de duplicación/moneda/reversal/periodos pasan.
- Refresh sobre mismo snapshot produce mismo hash/cifras.
- No LLM field alimenta dinero contable sin fuente validada.

## Mapa de escritura
- `packages/economics/`
- `packages/metrics/`
- `apps/web/src/`
- `supabase/`

No tocar private/, otros repos, credenciales, pruebas protegidas ni orquestador desde un candidato. Cambios de contratos/gates van en sesión de control, revisados antes de reanudar.

## Evidencia que se entrega
Comando exacto, código de salida, expected/observed, SHA, fixture hash y ruta de log/screenshot. Estados pass/fail/blocked/not_run; tests faltantes no equivalen a pass.

## Recuperación
Guardar diagnóstico y candidate worktree. No incorporar fallo al baseline. Reanudar desde checkpoint; no reiniciar import ni recalcular historia silenciosamente. Tras dos intentos fallidos, escalar con reproducción mínima.
