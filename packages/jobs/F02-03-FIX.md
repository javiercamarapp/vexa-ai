# F02-03 — corrección y recorrido real, 20-sep-2026

**Producto propuesto verificado localmente; no aceptado.** Llamada F02-29 / global173 según autorización, sin reset, delegación ni LLM adicional. Estado formal permanece12/60. F02-02 oficial y control-plane no se modificaron.

- task_id: F02-03.
- baseline_sha / HEAD sin cambios: `38a8c503cf691413fde27d385a47ade53273843e`.
- candidate_sha: no existe commit nuevo; cambios locales sin mutación Git. Identidad de contenidos: `/tmp/f0203-fix-final-source-hashes-v2.json`.
- fixture_sha256 (`packages/ingestion/mapping-fixtures.mjs`): `e48b954724e225c2236dddd312cae9be8722a9bde409406393b91e02c07600eb`.
- reviewer: autor de esta corrección; revisión independiente y gate oficial03 pendientes.

## Causa comprobada del bloqueo original

La reproducción sobre el build original conservado muestra que Chromium Docker sí alcanza el puente localhost: éste recibe `OPTIONS` con Origin `http://127.0.0.1:57911`, método solicitado PUT y header content-type. El Storage API aislado devuelve404 y ninguna cabecera Access-Control-Allow-Origin. Chromium informa explícitamente bloqueo CORS y cancela el PUT con net::ERR_FAILED. El PUT equivalente de Node devuelve200 porque Node no exige preflight. No fue un diagnóstico inferido sólo desde Failed to fetch ni una ausencia de conectividad localhost.

Evidencia original nueva: `/tmp/f0203-fix-diagnostic1.log`, recibo `run-29115623-44a4-4488-8705-6e922a91be95`, exit1/cleanup true, en el TMP original indicado abajo. Quedan intactos PROPOSAL y todos sus intentos históricos.

La corrección de transporte está exclusivamente en `test/mapping-fix/http.mjs`: el gateway HTTP de ensayo aplica CORS para el Origin propio y PUT/content-type. Un preflight no autorizado no obtiene permiso. Los bytes PUT, el objeto, Auth, PostgreSQL, Storage y respuestas de negocio siguen siendo reales. Se conserva la comparación directa `raw_storage OPTIONS404` frente a `gateway OPTIONS204`; el PUT de Chromium atraviesa el mismo proxy y Storage responde200. No hay response mocks, interceptación Playwright, disable-web-security ni cambios productivos para acomodar el proxy. Este ensayo no acredita configuración CORS cloud. `F02_RAW_GATEWAY=1` permite reproducir el transporte sin la corrección.

## Cambios de producto

- UI expone Más reservas y Más conexiones, acumula páginas sin duplicar IDs y conserva la conexión seleccionada de una segunda página al refrescar.
- XLSX descubre nombres reales con el parser existente. GET de detalle admite `?sheet=...`; la UI ofrece selector etiquetado y carga sus headers. Preview continúa rechazando multisheet sin selección explícita. Cambiar hoja requiere volver a configurar/validar/aprobar.
- Reintentar el mismo archivo/conexión conserva la clave y reserva durante la sesión del componente. Si Storage ya recibió los bytes, recupera el detalle en vez de crear otra reserva. Ante caducidad informa el error; una siguiente acción explícita renueva la reserva. Una importación queued no se vuelve a confirmar al repetir la carga.
- Cambiar mapping deshabilita confirmar. Tras rechazo de versión, capability obsoleta o caducidad se retira también la aprobación local; recargar obtiene el mapping vigente.
- Sin cambios en SQL0001–0005, servidor productivo de Storage/CORS, tests/acceptance, orchestration, registry, guía, Git, datos privados o configuración externa.

## Verificación y artefactos

Build/servicios/dependencias y logs quedaron en TMP. No se sobrescribió evidencia previa.

| Comando | Esperado / observado | Artefacto |
|---|---|---|
| `node --test packages/ingestion/*.test.mjs packages/jobs/test/mapping-fix/discovery.test.mjs` | exit0,96/96 CSV/XLSX/calendario/DST/money/discovery | `/tmp/f0203-fix-pure2.log` |
| `PYTHONDONTWRITEBYTECODE=1 npm test` | exit0,24/24 (12+12) | `/tmp/f0203-fix-regression.log` |
| `PYTHONDONTWRITEBYTECODE=1 npm run test:controller` | exit0,110 tests OK,96.524s | `/tmp/f0203-fix-controller.log` |
| `PYTHONDONTWRITEBYTECODE=1 npm run graph:check` | exit0,consulta sin promover | `/tmp/f0203-fix-graph.log` |
| `npm run build --workspace @vexa/web` en TMP, Node22.22, npm configs vacías | exit0 después del último cambio de producto | `/tmp/f0203-fix-build4.log` |
| wrapper F02-durable `run.py node packages/jobs/test/mapping-fix/http.mjs`, Node22 | exit0,PRODUCT_HTTP_COMPLETE,cleanup true | `/tmp/f0203-fix-http6.log`, run `8d836ab8-9e1c-4e8b-9d4d-47c827a92ab4` |
| `git diff --check` | exit0 | ejecución local; HEAD sin cambios |

HTTP real: CAS simultáneo con dos mappings distintos produce200/409; refresh permite siguiente versión. Preview/mapping verifican owner/analyst permitidos, operator/viewer403, Origin ajeno/null/vacío403, anónimo401 y otro tenant404; export propio para los cuatro roles y rechazo ajeno/anónimo. Incluye bytes incorrectos, tamaño incorrecto y reintento, expiración, mismo archivo renovado y confirmación repetida con un solo job, RLS Storage, revocación, rollback por outbox y caída real/reinicio de Next. Cada fixture usa DB aislada y cambios SQL sólo en ese recurso propio.

Chromium real: paginación de más de50 reservas y100 conexiones, selección desde segunda página, carga CSV, repetición sin duplicar, texto sin HTML, preview1001USD, guardar, descarga cuyo contenido incluye INVALID_CALENDAR sin texto crudo, escritura autenticada competidora, confirmación con capability obsoleta403 y botón deshabilitado, guardado stale409, refresh al mapping vigente, confirmación queued y reload. Además carga XLSX real con dos hojas, selector/headers/preview/guardar/cambio de hoja; caducidad inyectada en la DB propia, confirm deshabilitado, error de retry y renovación explícita con bytes reales. La cuenta final de jobs crece exactamente uno en el recorrido browser; las cargas XLSX no confirmadas no crean jobs.

Los403/409 del browser son negativos deliberados y http7 identifica las rutas confirm/mapping correspondientes. También se capturó un404 inicial de consola sin URL atribuible en los eventos response; no se afirma consola sin errores ni se atribuye ese404 a una causa no comprobada. Ninguna aserción del recorrido falló. No se presenta queued como procesamiento completado.

## Historial nuevo conservado

- diagnostic1: fallo original CORS, exit1 y cleanup true.
- http1: bloqueo de infraestructura antes de crear servicios: faltaba el directorio padre de evidencia en la copia TMP. Log preservado; se creó el directorio en TMP, sin modificar el runner.
- http2: HTTP verde, navegador falla en selector exacto de la prueba (`0 !== 103`) antes de subir; exit1 y cleanup true. Se corrigió el locator, no el producto para ese fallo.
- http3: primer recorrido CSV/XLSX/CAS completo verde, cleanup true.
- http4: añade caducidad real desde browser, verde, cleanup true.
- http5: añade selección en segunda página, verde, cleanup true.
- http6: añade invalidación de aprobación por capability obsoleta, verde, cleanup true; build4 corresponde al producto final.
- http7: trazado adicional de respuestas browser, `F02_BROWSER_ONLY=1`, exit0/cleanup true, run `2b51b909-2b56-4b65-addb-e1137c4c8cb0`. Repite HTTP mapping/roles/CAS y todo browser; omite únicamente las regresiones extendidas ya verdes en http6. Producto sin cambios respecto de build4.

Logs `/tmp/f0203-fix-http{1..7}.log` y builds1..4 se conservan. Recibos nuevos con sources antes/después, result, journal0600 y cleanup se encuentran en `TMP_FINAL/tests/acceptance/support/F02-durable/evidence/`. El diagnóstico inicial está en `TMP_ORIGINAL/tests/acceptance/support/F02-durable/evidence/`. Docker pull never, UUID por recurso, DB sin puerto publicado y HTTP sólo57910–57949. Cleanup verifica ausencia de recursos propios; no se tocó shared vexa-local.

TMP_ORIGINAL: `/var/folders/l3/czqfpdq5057__w_l5dxpp5pc0000gn/T/vexa-f0203-jprx8mg3`.

TMP_FINAL: `/var/folders/l3/czqfpdq5057__w_l5dxpp5pc0000gn/T/vexa-f0203-fix-puz3ko7_`.

Reproducción: `/tmp/f0203-fix-build.py` crea una copia e instala offline ignore-scripts; crear allí `tests/acceptance/support/F02-durable/evidence/`, y ejecutar el wrapper con `F02_PRODUCT_TMP=<copia>`, PATH de Node22 y el comando HTTP de la tabla. Pruebas nuevas versionables exclusivamente en `packages/jobs/test/mapping-fix/`.

## No probado / límites

Sin gate oficial03, mutantes externos, revisión independiente, aceptación ni promoción. No producción/cloud, CORS del proyecto remoto, proveedor externo, datos cliente, Office universal ni procesamiento durable posterior de F02-04/05/06. El retry conservado vive en la sesión del componente: después de recargar la página se debe abrir la reserva existente; no se promete reanudación de bytes ni recuperación automática de una carga interrumpida entre sesiones. No se midieron carreras de revocación durante cada descarga en vuelo. Las pruebas propias no reemplazan el examen externo.
