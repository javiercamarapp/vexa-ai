import test from 'node:test';import {launch,candidateInputs} from '../../tests/acceptance/support/F01-03/harness.mjs';import {seed} from '../../tests/acceptance/support/F01-03/oracles.mjs';import * as snapshots from '../../tests/acceptance/support/F01-03/snapshot-oracles.mjs';import * as priority from '../../tests/acceptance/support/F01-03/priority-oracles.mjs';
test('F05-06 historical input restoration creates new published version',{timeout:120000},async t=>{const h=await launch({services:true});t.after(()=>h.close());for(const sql of candidateInputs(process.env.VEXA_CANDIDATE))h.sql(sql);const actors={};for(const key of ['a','b','dual','outsider','viewer','analyst','operator'])actors[key]=await h.user();const base=seed(h,actors),snaps=snapshots.seed(h,base,actors),f=priority.seed(h,base,actors,snaps);priority.rollbackPolicy(h,f,actors);});
